# shellcheck shell=bash
# Machine-local shell settings, sourced last by both ~/.bashrc and ~/.zshrc.
#
# This is a chezmoi "create_" file: chezmoi seeds it once if it does not exist
# and never overwrites it afterwards, so anything you add here stays local to
# this machine and is not tracked by the dotfiles repository.
